import React from "react";
import ProductBlock from "../components/blocks/ProductPage/ProductBlock";

const Product: React.FC = () => {
	return (
		<section className="container">
			<ProductBlock />
		</section>
	);
};

export default Product;
